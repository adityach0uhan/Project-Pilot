import Loader from '@/components/ui/base/Loader';
import React from 'react';

const loading = () => {
    return (
        <div>
            <Loader />
        </div>
    );
};

export default loading;
